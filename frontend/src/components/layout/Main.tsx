import { DashboardPlaceholder } from './DashboardPlaceholder';

export const Main = () => {
  return (
    <main className="flex flex-col min-h-[calc(100vh-16px)]">
      <DashboardPlaceholder />
    </main>
  );
};